// Libs imports
import { Tool } from "@modelcontextprotocol/sdk/types.js";

export const sap_disconnect: Tool = {
  name: "sap_disconnect",
  description: "Disconnect from SAP OData service",
  inputSchema: {
    type: "object",
    properties: {},
  },
};
