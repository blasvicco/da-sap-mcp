services:
  sap-mcp:
    build:
      context: .
      dockerfile: ./hub/node/Dockerfile.local
    command: 'tail -f /dev/null'
    container_name: sap-mcp
    env_file:
      - ./app/.env.dev
    networks:
      shared_orb_net:
        aliases:
          - sapmcp.blas.local
    ports:
      - '3001:3001'
    volumes:
      - ./app:/home/app

networks:
  shared_orb_net:
    name: da-orb_da_orb_net
    external: true
